---
export interface Props {
	body: string;
	href: string;
}

const { body, href } = Astro.props;
---

<a href={href} class="w-[18rem] font-medium border-2 border-violet-300 rounded-md bg-violet-300 text-gray-800 py-2 hover:bg-fuchsia-100 hover:border-violet-400 hover:border-2">{body}</a>

